struct Foo {
    bar: i32;
}

fn identity(n: i32) -> i32 {
    return n;
}

fn identity_foo(a: Foo) -> Foo {
    return a;
}

fn main() -> i32 {
    let a := 32;

    let boo := identity_foo(new Foo { bar: a });

    return identity(a);
}